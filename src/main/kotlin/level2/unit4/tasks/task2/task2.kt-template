package level2.unit4.tasks.task1


// You have to write function myRepeat which works
// exactly like usual Kotlin repeat loop.

// --------------------------
// Write your code between these two lines. Do not change any code outside it.
// --------------------------


fun level1.unit2.examples.main() {
    var n = 0

    myRepeat(5) {
        n++
    }

    run {
        val expected = 5

        if (n == expected)
            println("passed.")
        else
            println("Failed.\nExpected: $expected\nYou made: $n")
    }
}
