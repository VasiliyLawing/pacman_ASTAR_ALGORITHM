package ch1.s2
/*
fun main() {
    for (n in 1 .. 10) {
        // When use nested 'if' better use {}
        // because 'else' belongs to the last 'if'. And it maybe not what you expect.
        if(n % 2 == 0)
            if(n > 5)
                println("$n - even")

            else
                println("$n - odd")
    }
}
*/
